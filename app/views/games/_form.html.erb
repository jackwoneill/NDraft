<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :team_1 %><br>
    <%= collection_select(:game, :team_1, @teams, :id, :name, {:prompt => true}) %>
  </div>
  <div class="field">
    <%= f.label :team_2 %><br>
    <%= collection_select(:game, :team_2, @teams, :id, :name, {:prompt => true}) %>
  </div>

  <div class="field">
    <label for="">Slate</label>
    <br>
    <%= collection_select(:game, :slate_id, @slates, :id, :name, {:prompt => true}) %>
  </div>
  <br>

  <div class="actions">
    <%= f.submit %>
  </div>



<% end %>
