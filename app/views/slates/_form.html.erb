<%= form_for(@slate) do |f| %>
  <% if @slate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slate.errors.count, "error") %> prohibited this slate from being saved:</h2>

      <ul>
      <% @slate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <p>ENUM THIS PART INTO COLLECTION SELECT</p>
    <%= f.label :game %><br>
    <%= f.number_field :game %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
